# زبان طراحی کارنو (Design System Brief)

خلاصهٔ هویت بصری و دیزاین‌سیستم صفحات اخیر (لندینگ افیلییت، ویجت انتخاب دوره، QR کارت، قرعه‌کشی، w500) — آماده برای تحویل به طراح گرافیک جهت طراحی لندینگ جدید.

---

## هویت بصری در یک جمله

**RTL، تمیز، گرم و «کاغذی»** — پس‌زمینهٔ کرم/استخوانی، برند سورمه‌ای، CTA قرمز تند، کارت‌های سفید با حاشیهٔ نرم و شبکهٔ ظریف در پس‌زمینه.

حس: آکادمیکِ مدرن + فروش مستقیم — نه لوکس سنگین و نه استارتاپ بنفش.

---

## پالت رنگ (Canonical)

| نقش | Hex | کاربرد |
|---|---|---|
| **Brand / Navy** | `#2D2C74` | لوگو، هایلایت تیتر، لینک، آیکون، تگ |
| **Brand Deep** | `#1F1E54` | گرادیان تیره، hover برند |
| **Brand Tint** | `#E7E6F3` | پس‌زمینهٔ pill، آیکون‌باکس، focus ring |
| **CTA / Red** | `#ED1C24` | دکمهٔ اصلی، هایلایت دوم تیتر، urgency |
| **CTA Deep** | `#C8151C` | hover دکمه |
| **CTA Tint** | `#FDE8E9` | badge هشدار/تخفیف |
| **Gold** | `#C8922A` | اکسنت محدود (یادداشت، جایزه، جزئیات طلایی) |
| **OK / Green** | `#138A4A` | موفقیت، ذخیره، چک |
| **OK Tint** | `#E6F4EC` | پس‌زمینهٔ موفقیت |
| **Paper** | `#F6F4EF` | پس‌زمینهٔ صفحه |
| **Paper 2** | `#EFEDE6` | سطح ثانویه |
| **Card** | `#FFFFFF` | کارت‌ها و فرم‌ها |
| **Ink** | `#14171E` | متن اصلی |
| **Ink 2** | `#2B313C` | متن نیمه‌قوی / لیبل |
| **Muted** | `#646C7C` | ساب‌تکست |
| **Line** | `#E6E2D8` | بوردر کارت |
| **Line 2** | `#D8D3C6` | بوردر قوی‌تر / dashed |

### قانون هایلایت در تیتر

- بخش اول / مفهوم برند → Navy (`hl`) → `#2D2C74`
- عدد / فوریت / فروش → Red (`hl2`) → `#ED1C24`

### متغیرهای CSS مرجع

```css
:root {
  --paper: #F6F4EF;
  --paper-2: #EFEDE6;
  --card: #FFFFFF;
  --ink: #14171E;
  --ink-2: #2B313C;
  --muted: #646C7C;
  --line: #E6E2D8;
  --line-2: #D8D3C6;
  --brand: #2D2C74;
  --brand-deep: #1F1E54;
  --brand-tint: #E7E6F3;
  --cta: #ED1C24;
  --cta-deep: #C8151C;
  --cta-tint: #FDE8E9;
  --gold: #C8922A;
  --ok: #138A4A;
  --ok-tint: #E6F4EC;
  --radius: 18px;
  --maxw: 1080px;
  --shadow: 0 1px 2px rgba(20, 23, 30, 0.04), 0 8px 24px rgba(20, 23, 30, 0.06);
  --shadow-lg: 0 24px 60px -20px rgba(45, 44, 116, 0.28), 0 12px 28px rgba(20, 23, 30, 0.10);
}
```

> نکته: در بعضی صفحات Paper کمی گرم‌تر است (`#F7F4F0` یا `#FAF6EF`) — همان خانوادهٔ کرم استخوانی؛ برای لندینگ جدید `#F6F4EF` را canonical بگیر.

---

## تایپوگرافی

| نقش | فونت | توضیح |
|---|---|---|
| **اصلی فارسی** | `Peyda` / `Peydavar` | ترجیح برند — وزن‌های ۱۰۰ تا ۹۰۰ |
| **جایگزین قابل قبول** | `Vazirmatn` | در بعضی لندینگ‌ها استفاده شده |
| **مونو / عدد / eyebrow** | `IBM Plex Mono` | درصد، کد، لیبل انگلیسی |

- **جهت:** همیشه RTL
- **خط‌ارتفاع بدنه:** ~۱.۸۵ تا ۱.۹۵
- **وزن‌ها:** بدنه ۶۰۰–۷۰۰ · تیتر ۸۰۰–۹۰۰ · CTA ۸۰۰
- **Smoothing:** antialiased

### مقیاس تیتر و متن

| سطح | اندازه تقریبی | وزن |
|---|---|---|
| Hero H1 | `clamp(28px, 5vw, 46px)` | ۹۰۰ |
| Section H2 | `clamp(24px, 4vw, 34px)` | ۹۰۰ · letter-spacing ~`-0.4px` |
| Card title | ۱۵–۲۰px | ۸۰۰ |
| Body / sub | ۱۵–۱۷px | عادی / ۶۰۰ · رنگ muted |
| Small / meta | ۱۲–۱۳.۵px | ۶۰۰–۷۰۰ |
| Eyebrow pill | ۱۲px · letter-spacing ۱.۵px | ۶۰۰ · uppercase |

---

## فاصله، گوشه، سایه، عرض

| مورد | مقدار |
|---|---|
| Max width محتوا | `1080–1100px` |
| Padding افقی | ~۲۰px |
| فاصله سکشن | ~۷۴px عمودی |
| Radius پایه | `18px` |
| Radius دکمه | `11–13px` |
| Radius کارت بزرگ | `20–24px` |
| Radius pill | `50px` (کامل) |
| Radius اینپوت | `12px` |
| Radius بلوک تیره / رجیستر | `28px` |

### سایه‌ها

- **نرم:** `0 1px 2px rgba(20,23,30,.04), 0 8px 24px rgba(20,23,30,.06)`
- **قوی:** `0 24px 60px -20px rgba(45,44,116,.28), 0 12px 28px rgba(20,23,30,.10)`
- **سایه CTA:** `0 10–12px 24–28px -8px rgba(237,28,36,.5–.65)`

### حرکت (Motion)

- Hover دکمه: `translateY(-2px)` + تیره‌تر شدن قرمز
- Hover کارت: `translateY(-3 / -4px)` + shadow
- ترنزیشن معمول: `~0.18–0.25s`

---

## بافت پس‌زمینه (امضای بصری)

1. **Radial wash:** لکه‌های خیلی کم‌رنگ Navy و Red در گوشه‌های بالا
2. **Grid ۵۰–۶۰px** با opacity حدود `0.32` و mask شعاعی از بالا تا محو شود
3. **بلوک‌های تیره** (فرم / CTA پایانی): گرادیان ink تیره + همان گرید سفید خیلی کم‌رنگ

نمونهٔ مفهومی:

```css
/* wash */
background:
  radial-gradient(820px 460px at 100% -5%, rgba(45, 44, 116, 0.07), transparent 60%),
  radial-gradient(640px 440px at -5% 6%, rgba(237, 28, 36, 0.05), transparent 55%);

/* grid */
background-image:
  linear-gradient(var(--line) 1px, transparent 1px),
  linear-gradient(90deg, var(--line) 1px, transparent 1px);
background-size: 60px 60px;
opacity: 0.32;
mask-image: radial-gradient(circle at 50% 0, #000, transparent 70%);
```

---

## کامپوننت‌های ثابت

### Eyebrow pill
- پس‌زمینه: brand-tint
- متن: brand
- گوشه کامل (pill)
- گاهی با فونت mono

### دکمه CTA
- پس‌زمینه قرمز پر (`#ED1C24`)
- متن سفید
- radius ۱۳
- وزن ۸۰۰
- سایه قرمز نرم

### دکمه Ghost
- سفید
- بوردر `1.5px` line-2
- متن ink
- hover: بوردر ink

### کارت
- سفید
- بوردر `1px` line
- radius ۱۸–۲۴
- گاهی نوار رنگی `4px` بالا (brand یا cta)

### آیکون‌باکس
- مربع گرد ~۴۰–۴۶px
- radius ۱۱–۱۳
- پس‌زمینه tint
- آیکون **stroke خطی** (نه پر)

### Tag / Badge
- pill کوچک
- ۱۰.۵–۱۲px
- پس‌زمینه tint رنگی

### اینپوت / فرم
- پس‌زمینه paper
- بوردر `1.5px`
- focus: بوردر brand + ring `4px` brand-tint
- لیبل: ۱۳px · وزن ۷۰۰ · ink-2
- علامت الزامی: رنگ cta

### FAQ
- کارت جمع‌شونده
- باز شدن → بوردر brand
- آیکون `+` می‌چرخد و پر می‌شود

### بلوک ثبت‌نام تیره
- گرادیان ink
- فرم سفید داخل
- دکمه قرمز تمام‌عرض

---

## رنگ‌های کمکی مرحله / مسیر (Roadmap)

در ویجت انتخاب دوره برای سه مرحله:

| مرحله | رنگ | Tint |
|---|---|---|
| Stage 1 | `#2D2C74` | `#E7E6F3` |
| Stage 2 | `#0E8C6A` | `#E2F4EE` |
| Stage 3 | `#ED1C24` | `#FDE6E7` |

---

## لحن و اصول برای لندینگ جدید

1. فولد اول تمیز باشد: **eyebrow + یک تیتر قوی + یک ساب کوتاه + گروه CTA**
2. اعداد و درصدها برجسته باشند (mono یا وزن خیلی بالا) با رنگ brand / cta
3. کارت زیاد نشود مگر برای تعامل یا مقایسه
4. از این‌ها پرهیز شود: بنفش گرادیانی، dark-mode پیش‌فرض، glow نئونی، خوشهٔ pillهای زیاد
5. تصویر واقعی محصول/فضا اگر هست؛ وگرنه بافت کاغذ + گرید کافی است
6. کنتراست CTA قرمز روی کاغذ کرم باید همیشه واضح بماند
7. یک کار در هر سکشن: یک تیتر، معمولاً یک جملهٔ پشتیبان

---

## فایل‌های مرجع در همین فولدر

| فایل | نقش |
|---|---|
| `carno-affiliate-landing.html` | کامل‌ترین لندینگ مستقل |
| `karno-affiliate-elementor.html` | نسخهٔ Elementor همان زبان |
| `choose-widget-temp.html` | کارت محصول، مودال، roadmap |
| `qr-card-landing-widget.html` | نسخهٔ مینیمال لینک‌کارت |
| `قرعه کشی/raffle.html` | تم Peyda + حالت dark اختیاری |
| `w500.html` | ویجت وبینار با همان توکن‌ها |

---

## چک‌لیست سریع برای طراح

- [ ] پس‌زمینه کرم کاغذی (`#F6F4EF`) نه سفید خالص
- [ ] برند سورمه‌ای `#2D2C74` + CTA قرمز `#ED1C24`
- [ ] فونت فارسی Peyda (یا Peydavar)
- [ ] RTL کامل
- [ ] گوشه‌های نرم ۱۸px+ برای کارت‌ها
- [ ] گرید/radial wash خیلی ظریف در پس‌زمینه
- [ ] دکمهٔ اصلی قرمز با سایهٔ قرمز، نه بنفش/آبی
- [ ] تیتر وزن ۹۰۰؛ هایلایت دو رنگ (navy + red)
- [ ] بدون کارت در هیرو مگر لازم برای تعامل
